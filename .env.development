# Solo desarrollo local: el dev server no tiene Pages Functions, pega directo
# al worker (o a `wrangler dev` en :8787 cuando el worker público se cierre).
VITE_WORKER_URL=https://speechify-tts.ernestoqfb.workers.dev
