# Handoff — Folio (audiolector) · 2026-07-22 · PRÓXIMA SESIÓN: AUDITORÍA SERIA

## Contexto de este handoff

Ernesto reporta que **hay varios errores** tras el maratón de features del 21-jul y pide una **auditoría seria** como siguiente paso. Este documento da el mapa del terreno y las zonas calientes. Ficha general del proyecto: memoria `folio_audiolector.md`; handoff anterior: `handoffs/handoff_2026-07-16_222713.md`. **Cuenta Cloudflare PERSONAL (wrangler OAuth) — NUNCA la de DISA.**

## Estado al corte

- Producción `folio.thestandardcurve.com`, último commit `9942029`, árbol limpio, main pusheado, deploy `2ad83bf8` verificado (302 Access). Deploy manual: `npm run build && npx wrangler pages deploy dist --project-name=tss-reader-app`.
- La sesión del 21-jul metió MUCHO en un día (ver `git log` desde `7c8037c`): sync por identidad + auto-sync, descarga offline por capítulo, rebranding speechify→folio con migración de llaves, polyfills WebKit (ReadableStream asyncIterator, withResolvers), pre-encolado gapless en el motor clásico, **motor MSE nuevo para Android** (`src/agents/mse-player.ts`, selección por capacidad en `player.ts`, escape `localStorage folio-engine=classic`), Media Session con positionState, SW en modo prompt + UpdateToast, iconos SVG (`components/icons.tsx`), y seguimiento de scroll por palabra en párrafos largos (`ReaderView.tsx`).

## Zonas calientes para la auditoría (dónde buscar los errores)

1. **PlayerBar.tsx es el epicentro de riesgo.** Acumuló en un día: cadena de chunks + pre-encolado (`queueUpcoming`, `pendingNextChainRef`), recuperación por cambio de generación (corta audio huérfano + re-arranque), skip de párrafos sin voz, positionState, y dos motores debajo. Los bugs que YA se cazaron ahí eran todos del mismo género: **estado huérfano en carreras de generación** (isBuffering atorado, store desincronizado del audio, chunkStart con gen vieja). Auditar sistemáticamente TODOS los caminos async contra: cambio de voz/velocidad, next/prev/capítulo, seek por click en párrafo, cierre del libro, y combinaciones a media carga.
2. **MseEngine (`mse-player.ts`)** es código de 1 día con 2 fixes ya encima (timeupdate porque rAF se congela en background; fin-de-stream por `waiting`/`stalled`). Revisar: eviction por QuotaExceeded (¿re-base de startMs tras remove?), seeks (load() resetea el stream — ¿posición correcta?), `activeIndex` vs segments crecientes, memoria (segments nunca se poda), y el contrato getCurrentPositionMs en pausa/resume.
3. **auto-sync (`lib/auto-sync.ts`)**: los PUT a `/sync/me` fallaban en dev en ráfagas (ERR_FAILED visto en QA — nunca investigado a fondo). Verificar: comportamiento offline real, el snapshot de 64KB (¿bibliotecas grandes lo revientan? el cap es del worker), y que el merge LWW no pierda progreso con reloj desfasado entre dispositivos.
4. **Choque potencial gapless × auto-scroll × detached**: tres features nuevas comparten eventos (visibilitychange, scroll, wordIndex). Buscar interacciones no probadas (p.ej. seguimiento por palabra durante el modo despegado al cambiar de capítulo).
5. **Los errores concretos de Ernesto: PEDIRLE la lista al arrancar** — este handoff se escribió sin los síntomas específicos. Su iPhone = motor clásico; su Android (Brave) = MSE. La versión que corre cada dispositivo importa: el UpdateToast es nuevo y el lag de SW ya causó 3 falsos negativos — SIEMPRE confirmar versión antes de diagnosticar.

## Cómo reproducir/verificar (recetario probado en la sesión)

- Preview: `preview_start` name `folio` (5173) o `folio-qa` (5199, origen distinto = biblioteca IndexedDB separada). Worker TTS local: `cd worker && npx wrangler dev --port 8787` (listo cuando GET /tts → 405).
- Inyectar libros: generar PDF sintético a mano en python (patrón en la sesión: PDF 1.4 con líneas `BT/Tj`; línea terminada en coma = se une al párrafo anterior, en punto = párrafo nuevo; un párrafo ". . ." replica separadores) → `public/__qa-*.pdf` → en la página `fetch` + `DataTransfer` sobre `input[type=file]`. Borrar el PDF de public/ al final.
- Instrumentación clave: los módulos reales se importan en la página vía Vite (`await import('/src/store/playback-store.ts')`); trazar con `usePlaybackStore.subscribe`. OJO: PlayerBar captura algunos setters por hook (patch de setParagraphTiming/setBuffering NO se ve); los `getState().x()` sí.
- Safari real vía safaridriver (WebDriver, puerto 4444+): Ernesto debe activar "Permitir automatización remota" (se le des-activa a veces). Los clicks de WebDriver NO cuentan como gesto para autoplay — pausa honesta ≠ bug. Chromium del panel: `setSpeed`/clicks JS tampoco llevan gesto.
- El libro Kundera (`epub-p-*` ids) y Profit First viven en la nube de sync de Ernesto; el PDF de anemia de la tester ya no está en Downloads.

## Pendientes NO-auditoría (no perder)

- Confirmación del usuario del S24 (motor MSE en su equipo + sacar navegador de apps dormidas) y de la tester del PDF (fix Safari ya publicado).
- Rebranding restante: bucket R2 `speechify-tts-cache` (datos prod), borrar worker viejo `speechify-tts` del dashboard cuando se despliegue `folio-tts`, renombrar carpeta local y repo GitHub al cerrar nombre comercial (candidato: **Folio Audio**; "Folio Reader" NO viable — framework FolioReader).
- Backlog handoff anterior: texto CSS de `<style>` como párrafos en ePubs, PDFs con palabras pegadas, títulos "Unknown"/"Untitled Document" en capítulos.

## Skills sugeridas

- `/reanudar` — para retomar este handoff.
- `/code-review` (nivel high) sobre el diff acumulado del 21-jul (`git diff 6f98683..9942029`) — es la auditoría de código formal; considerar `ultra` si Ernesto quiere profundidad de nube.
- `/diagnose` — para cada error concreto que Ernesto reporte, antes de tocar código.
- `/qa` o `/qa-only` — pasada sistemática de la app en preview.
- `/publicar` — el ciclo de cierre (con sus smoke checks; el push puede fallar a la primera si cambió de red — reintentar).
- Modo orquestador ("multimodelo") — Ernesto tiene el atajo en memoria: Fable orquesta/audita, Opus implementa; útil si la auditoría abre varios frentes.
